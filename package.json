{
  "name": "gh-feedback",
  "version": "1.0.0",
  "description": "Read, reply, react, and resolve GitHub PR feedback on the current branch",
  "author": "Łukasz Jerciński",
  "license": "MIT",
  "scripts": {
    "prepack": "pnpm run rebuild",
    "prepare": "git config core.hooksPath .githooks",
    "build": "tsc -p tsconfig.app.json",
    "clean": "rm -rf dist *.tsbuildinfo",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "fta": "fta-check",
    "knip": "knip",
    "lint": "eslint",
    "rebuild": "pnpm run clean && pnpm run build",
    "start": "pnpm -s run rebuild && node --env-file=.env bin/gh-feedback",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "test:watch": "vitest",
    "typecheck": "tsc -b --noEmit"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Jercik/gh-feedback.git"
  },
  "type": "module",
  "bin": {
    "gh-feedback": "bin/gh-feedback"
  },
  "files": [
    "bin/",
    "dist/",
    "README.md",
    "LICENSE"
  ],
  "keywords": [],
  "packageManager": "pnpm@10.33.0",
  "engines": {
    "node": ">=22.19.0"
  },
  "dependencies": {
    "@commander-js/extra-typings": "^14.0.0",
    "commander": "^14.0.3"
  },
  "devDependencies": {
    "@total-typescript/ts-reset": "^0.6.1",
    "@types/node": "^25.6.0",
    "@vitest/coverage-v8": "^4.1.4",
    "eslint": "^10.2.0",
    "eslint-config-axkit": "^1.6.3",
    "fta-check": "^1.5.1",
    "fta-cli": "^3.0.0",
    "knip": "^6.3.1",
    "prettier": "3.8.2",
    "semantic-release": "^25.0.3",
    "typescript": "^6.0.2",
    "vitest": "^4.1.4"
  }
}
